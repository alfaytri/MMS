// src/components/purchase/ApprovalRoleAssignmentsTab.tsx
'use client'

/**
 * TEMPORARY STUB — Task 11 of unified-roles plan removed the underlying
 * `useApprovalRoleAssignments` hook (and the `approval_role_assignments`
 * table is being dropped). A subsequent task will rewrite this tab against
 * the new unified `custom_roles` + `user_custom_roles` (approval-slot) model.
 */
export function ApprovalRoleAssignmentsTab() {
  return (
    <div className="rounded-lg border border-dashed p-6 text-center text-sm text-muted-foreground">
      Approval role assignments are being migrated to the unified roles system.
      This tab will return in a follow-up task.
    </div>
  )
}
